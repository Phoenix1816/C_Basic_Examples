#include <stdio.h>
#include <stdlib.h>

int islem(int a,int b)
{
    int toplam,fark,carpim,bolum;
    toplam=a+b;
    fark=a-b;
    carpim=a*b;
    bolum=a/b;
}






int main()
{
    int a,b,toplam;
    printf("Lutfen iki sayi giriniz:");
    scanf("%d%d", &a, &b);
    printf("%d", islem(a,b,toplam));
     printf("%d", islem(a,b));
      printf("%d", islem(a,b));
       printf("%d", islem(a,b));
}
