#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,n,k,i;
    printf("Kac ogrenci olacagini giriniz:");
    scanf("%d", &k);
    for(i=0;i<k;i++)
    {
    printf("Ogrenci numaranizi ve boyunuzu giriniz:");
    scanf("%d%d", &a, &b);
    n== a-150;
        if(b<b)
    }
}
